/**
 * 對 openid 進行 加密 和 解密 的 函數, 由於某個追加, 此函數暫停研發
 */

// 加密
function encrypt (openid) {
    return openid
}

// 解密
function decrypt (openid) {
    return openid
}

export {
    encrypt,
    decrypt
}
